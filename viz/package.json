{
  "name": "agora-viz",
  "version": "0.1.0",
  "private": true,
  "description": "AGORA browser-native 3D visualization layer",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
